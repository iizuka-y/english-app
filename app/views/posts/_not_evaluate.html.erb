<%= form_for current_user.evaluations.build, url: post_evaluations_path(post.id), html: { method: :post, id: "#{post.id}(1)" }, remote: true do |f| %>
  <div><%= hidden_field_tag :post_id, post.id, id: "#{post.id}1" %></div> <!--post_idをコントローラーに送信するためにhiddenで隠し持つ-->
  <div><%= hidden_field_tag :star_count, 1, id: "#{post.id}1s" %></div> <!--star_countをコントローラーに送信するためにhiddenで隠し持つ-->
  <%= image_submit_tag "star0.png", class: "star" %>
<% end %>
<%= form_for current_user.evaluations.build, url: post_evaluations_path(post.id), html: { method: :post, id: "#{post.id}(2)" }, remote: true do |f| %>
  <div><%= hidden_field_tag :post_id, post.id, id: "#{post.id}2" %></div> <!--post_idをコントローラーに送信するためにhiddenで隠し持つ-->
  <div><%= hidden_field_tag :star_count, 2, id: "#{post.id}2s" %></div> <!--star_countをコントローラーに送信するためにhiddenで隠し持つ-->
  <%= image_submit_tag "star0.png", class: "star" %>
<% end %>
<%= form_for current_user.evaluations.build, url: post_evaluations_path(post.id), html: { method: :post, id: "#{post.id}(3)" }, remote: true do |f| %>
  <div><%= hidden_field_tag :post_id, post.id, id: "#{post.id}3" %></div> <!--post_idをコントローラーに送信するためにhiddenで隠し持つ-->
  <div><%= hidden_field_tag :star_count, 3, id: "#{post.id}3s" %></div> <!--star_countをコントローラーに送信するためにhiddenで隠し持つ-->
  <%= image_submit_tag "star0.png", class: "star" %>
<% end %>
