<!--ログインユーザーが投稿を評価している場合-->
<% if post.evaluated_by?(current_user) %>
  <%= render 'posts/evaluate', post: post %>
<% else %>
<!--ログインユーザーが投稿を評価してない場合-->
  <%= render 'posts/not_evaluate', post: post %>
<% end %>
